import { useContext, useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { AuthContext } from "../context/AuthContext";
import { AuthCard, Field } from "./Register";
const VerifyOtp = () => { const { verifyOtp } = useContext(AuthContext); const navigate = useNavigate(); const email = sessionStorage.getItem("otpEmail") || ""; const [otp, setOtp] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false);
 const submit = async (event) => { event.preventDefault(); if (!email) return setError("Your registration email is missing. Please register again."); if (!otp.trim()) return setError("Please enter the OTP sent to your email."); setError(""); setLoading(true); try { await verifyOtp(email, otp.trim()); sessionStorage.removeItem("otpEmail"); navigate("/login"); } catch (err) { setError(err.response?.data?.message || "Unable to verify the OTP. Please try again."); } finally { setLoading(false); } };
 return <AuthCard title="Verify your email" subtitle={email ? `We sent a one-time password to ${email}.` : "Enter the one-time password sent to your email."}><form onSubmit={submit} className="space-y-5"><Field label="One-time password" type="text" value={otp} onChange={setOtp} autoComplete="one-time-code" />{error && <p role="alert" className="rounded-lg bg-red-50 px-4 py-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="w-full rounded-xl bg-gray-950 px-4 py-3 font-semibold text-white hover:bg-gray-700 disabled:cursor-not-allowed disabled:opacity-60">{loading ? "Verifying..." : "Verify OTP"}</button></form><p className="mt-6 text-center text-sm text-gray-600">Need to start over? <Link to="/register" className="font-semibold text-gray-950 hover:underline">Register</Link></p></AuthCard>; };
export default VerifyOtp;
